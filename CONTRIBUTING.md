# 작업 규약 — jinsang-stop-backend

Java Spring + MySQL. `훈련 세션` 오케스트레이션, `민원인` 연기·채점 프롬프트 조립, 카드 저장소, 성장 집계를 소유한다.

제품·도메인 문서(용어집·ADR·PRD)는 이 저장소가 아니라 [jinsang-stop/jinsang-stop](https://github.com/jinsang-stop/jinsang-stop)에 있다.
클래스·필드·API·DB 이름은 그 `CONTEXT.md`의 용어를 그대로 쓰고, `_Avoid_`로 적힌 동의어를 쓰지 않는다.
ADR과 어긋나는 구현이 필요해 보이면 코드로 덮지 말고 이슈에 먼저 올린다.

## 라벨

이슈·커밋·PR에 공통으로 쓴다.

| 라벨 | 뜻 |
|---|---|
| `Feat` | 기능 |
| `Fix` | 버그 |
| `Refactor` | 구조 |
| `Design` | UI |
| `Chore` | 빌드 · 의존성 |
| `Setting` | 환경설정 |
| `Comment` | 주석 · 로그 |
| `Docs` | 문서 |
| `Test` | 테스트 |

트리아지 라벨 `needs-triage` · `needs-info` · `ready-for-agent` · `ready-for-human` · `wontfix`는 작업 종류 라벨과 함께 붙인다.

## 이슈 — 구현보다 먼저 연다

- 제목: `[Label] {이슈번호} {목표}` — 예: `[Feat] 3 공식 카드 목록 조회 API`
- 본문은 이슈 템플릿(목적 / 범위 / 작업 내용 / 완료 조건) 그대로 쓴다. **섹션을 더하거나 빼지 않는다.**
- 작업 내용과 완료 조건은 체크박스로, 완료 조건은 눈으로 확인 가능한 문장으로 쓴다.
- 슬라이스에서 나온 작업은 `jinsang-stop/jinsang-stop`의 `[Slice]` 이슈에 하위 이슈로 연결돼 있다. 공통 계약(필드·상태·오류 전달)은 슬라이스 이슈가 정본이다.

## 브랜치

- **이슈 하나당 브랜치 하나.** 형식은 `<라벨 소문자>/#<이슈번호>` — `feat/#3`, `fix/#12`
- `main`에 직접 push할 수 없다. 저장소 규칙으로 PR을 거쳐야만 병합되고, force push와 삭제가 막혀 있다.

## 커밋

- **기능 하나가 끝날 때마다** 커밋한다. 여러 기능을 한 커밋에 몰아 담지 않는다.
- 형식: `[Label/#이슈번호] Label: 메시지`

```
[Feat/#3] Feat: 공식 카드 목록 조회 API 구현
[Fix/#12] Fix: 빈 전사 결과가 발화 턴을 소모하던 문제 수정
```

## PR

- 제목: `[Label/#이슈번호] 작업 요약` — 예: `[Feat/#3] 공식 카드 목록 조회 API 구현`
- 본문은 PR 템플릿(연관 Issue / 요약 / 변경 사항 / 범위 / 스크린샷·미리 보기) 그대로 쓴다. **임의 섹션을 추가하지 않는다** — 더 적을 게 있으면 `## 요약`이나 `## 변경 사항` 안에 녹인다.
- `closes #N`으로 이 저장소 이슈를 닫는다. 상위 슬라이스는 모든 파트가 닫힌 뒤 통합 검증을 하고 따로 닫는다.
- 병합하면 브랜치는 자동으로 삭제된다.

## 진행 로그

작업을 중단하거나 한 단계를 마칠 때 해당 이슈에 댓글을 남긴다 — **한 것 / 남은 것 / 다음에 할 것 / 막힌 것.**
다음에 작업하는 사람(또는 세션)은 `gh issue view <번호> --comments`로 상태를 복원한다.

## 백엔드 규칙

- **모든 코드에 한글 주석**을 단다. 각 클래스·메서드가 무슨 기능을 담당하는지 읽으면 알 수 있게 쓴다.
- **API 문서 어노테이션을 전부 붙인다(springdoc).** 컨트롤러와 DTO 모두. `/swagger-ui`에 실제로 반영되는지 확인한 뒤 PR을 올린다.
- PR에는 Postman으로 확인할 수 있게 **요청 URL · 메서드 · 헤더 · 바디 · 기대 응답**을, DB 확인이 필요하면 실행할 쿼리까지 `## 변경 사항` 안에 적는다.
- `음성 서비스`와 `추론 서비스` 주소, 모델 경로는 환경변수로 주입한다. 코드에 박지 않는다.
- 프론트로 내려가는 응답에 `시나리오 카드`의 진정 조건과 양보 불가선을 넣지 않는다.
- 채점 실패는 0점이 아니라 `정보 없음`으로 저장한다.
- 사용자 음성 파일은 전사 직후 폐기하고 DB·디스크에 남기지 않는다(ADR-0006). 서버에 남는 것은 전사 텍스트, 축별 점수, `소요 턴 수`, 종료 사유까지다.
